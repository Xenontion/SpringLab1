<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
          http://www.springframework.org/schema/beans
          https://www.springframework.org/schema/beans/spring-beans.xsd
          http://www.springframework.org/schema/context
          https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- підключаємо properties -->
    <context:property-placeholder location="classpath:university.properties"/>

    <!-- 1-й рівень: Pet -->
    <bean id="myPet" class="org.chdtu.Cat">
        <property name="name" value="${pet.name}"/>
    </bean>

    <!-- 2-й рівень: Person, який має Pet -->
    <bean id="myPerson" class="org.chdtu.Person">
        <property name="name" value="${person.name}"/>
        <property name="surname" value="${person.surname}"/>
        <property name="age" value="${person.age}"/>
        <property name="pet" ref="myPet"/>
    </bean>

    <!-- 3-й рівень: University, який має Person -->
    <bean id="myUniversity" class="org.chdtu.University">
        <constructor-arg value="${university.name}"/>
        <constructor-arg ref="myPerson"/>
    </bean>

</beans>
